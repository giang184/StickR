<h1>Add a new image:</h1>

<%= form_for [@user, @image] do |f| %>
  <%= f.label :url %>
  <%= f.text_field :url %>
  <%= f.label :description %>
  <%= f.text_area :description %>
  <p><%= f.submit %></p>
<% end %>

